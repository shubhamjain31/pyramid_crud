<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <ul class="navbar-nav">

            {% if request.authenticated_userid %}

            <span class="navbar-brand mb-0 h1">Admin</span>
            {% else %}
            <span class="navbar-brand mb-0 h1">User</span>

            {% endif %}
            <li class="nav-item">
                <a class="nav-link" href="{{request.route_url('home')}}">Home</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{request.route_url('bookingform')}}">Book ticket</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{request.route_url('mybooking')}}">My Bookings</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{request.route_url('addTrain')}}">Add Trains</a>
            </li>

            {% if request.authenticated_userid %}
            <li class="nav-item">
                <a class="nav-link" href="{{request.route_url('logout')}}">Logout</a>
            </li>
            {% else %}

            <li class="nav-item">
                <a class="nav-link" href="{{request.route_url('login')}}">Login</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{request.route_url('register')}}">Register</a>
            </li>

            {% endif %}
        </ul>
    </nav>